// This footer will appear on all pages
const Footer = () => {

  return (
    <div style={{backgroundColor: 'lightgrey'}}>This is the Footer</div>
    // Try not to use html elements here - surely there is a Semantic ui component suitable here!
  );
};

export default Footer;